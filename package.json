{
  "name": "restaurant-ai-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Agente de voz (Vapi) + WhatsApp (Twilio) para restaurante, backend compartido en Express.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "build": "npm --prefix app install --include=dev --no-audit --no-fund && npm --prefix app run build"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "twilio": "^5.3.4"
  },
  "engines": {
    "node": ">=18"
  }
}
